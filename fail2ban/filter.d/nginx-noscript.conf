# Fail2Ban filter for script injection attempts
# Blocks attempts to access script files that shouldn't exist

[Definition]
failregex = ^<HOST> -.*"(GET|POST).*(\.php|\.asp|\.aspx|\.jsp|\.cgi|\.pl|\.py|\.rb|\.sh|\.bash).*HTTP.*".*$

ignoreregex =
