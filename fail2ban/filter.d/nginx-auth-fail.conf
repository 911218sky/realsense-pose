# Fail2Ban filter for authentication failures
# Blocks brute force login attempts (401/403 responses)

[Definition]
failregex = ^<HOST> -.*"(GET|POST|PUT|DELETE).*HTTP.*" (401|403) .*$

ignoreregex =
